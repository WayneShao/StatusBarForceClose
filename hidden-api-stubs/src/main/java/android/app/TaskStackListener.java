package android.app;

/** Compile-only signature stub. The Android boot class path supplies the runtime class. */
public class TaskStackListener {
    public void onTaskMovedToFront(ActivityManager.RunningTaskInfo taskInfo) {
    }
}
